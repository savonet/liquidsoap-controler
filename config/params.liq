# Configuration file for the controler.

# Global settings
set("log.file",   false)
#set("log.file.path", "/path/to/log.txt")
set("log.stdout", true)
set("log.level", 4)

# Port where the json controler is running on.
controler.port     = 8080
controler.user     = "test"
controler.password = "test"
